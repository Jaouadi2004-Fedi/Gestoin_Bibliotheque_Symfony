<div class="card border-0 bg-danger-subtle mt-4">
    <div class="card-header bg-transparent border-0 text-danger">
        <h5 class="mb-0 fw-bold">
            <i class="fas fa-exclamation-triangle"></i>
            Zone de danger
        </h5>
    </div>
    <div class="card-body">
        <p class="text-danger mb-3">Une fois que vous supprimez cet enregistrement, il n'y a pas de retour en arrière. Soyez certain.</p>
        <form method="post" action="{{ path('app_borrowing_delete', {'id': borrowing.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet emprunt ? Cette action est irréversible.');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ borrowing.id) }}">
            <button type="submit" class="btn btn-outline-danger">
                <i class="fas fa-trash-alt"></i> Supprimer cet emprunt
            </button>
        </form>
    </div>
</div>


