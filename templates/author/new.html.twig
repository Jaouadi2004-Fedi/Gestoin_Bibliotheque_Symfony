{% extends 'base.html.twig' %}

{% block title %}Nouvel Auteur{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="mb-4 text-center">
            <h2 class="fw-bold text-dark">Créer un nouvel auteur</h2>
            <p class="text-muted">Remplissez les détails ci-dessous pour ajouter un nouvel auteur</p>
        </div>
        
        {{ include('author/_form.html.twig') }}
        
        <div class="mt-3 text-center">
             <a href="{{ path('app_author_index') }}" class="text-decoration-none text-muted">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>
        </div>
    </div>
</div>
{% endblock %}


