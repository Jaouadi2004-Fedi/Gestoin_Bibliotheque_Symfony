{{ form_start(form, {'attr': {'class': 'form-card bg-white'}}) }}
<div class="card-header bg-white border-bottom">
    <h3 class="text-dark">
        <i class="fas fa-user-tie text-primary"></i>
        {{ button_label|default('Nouvel Auteur') == 'Nouvel Auteur' ? 'Nouvel Auteur' : 'Modifier l\'auteur' }}
    </h3>
</div>

<div class="row">
    <div class="col-md-6">
        <div class="form-group">
            <div class="form-label text-muted">
                <i class="fas fa-user"></i>
                <span>{{ form_label(form.name) }}</span>
            </div>
            {{ form_widget(form.name, {'attr': {'class': 'form-control', 'placeholder': 'Entrez le prénom de l\'auteur'}}) }}
            {{ form_errors(form.name) }}
        </div>
    </div>
    <div class="col-md-6">
        <div class="form-group">
            <div class="form-label text-muted">
                <i class="fas fa-signature"></i>
                <span>{{ form_label(form.surname) }}</span>
            </div>
            {{ form_widget(form.surname, {'attr': {'class': 'form-control', 'placeholder': 'Entrez le nom de l\'auteur'}}) }}
            {{ form_errors(form.surname) }}
        </div>
    </div>
</div>

<div class="form-actions text-end border-top pt-3 mt-4">
    <a href="{{ path('app_author_index') }}" class="btn btn-cancel me-2">
        <i class="fas fa-times"></i> Annuler
    </a>
    <button type="submit" class="btn btn-submit">
        <i class="fas fa-save"></i> {{ button_label|default('Enregistrer') }}
    </button>
</div>

{{ form_end(form) }}


