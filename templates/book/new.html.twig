{% extends 'base.html.twig' %}

{% block title %}Nouveau Livre{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="mb-4 text-center">
            <h2 class="fw-bold text-dark">
                <i class="fas fa-book-medical text-primary"></i>
                Ajouter un nouveau livre
            </h2>
            <p class="text-muted">Remplissez les détails ci-dessous pour ajouter un nouveau livre</p>
        </div>
        
        {{ include('book/_form.html.twig') }}
        
        <div class="mt-3 text-center">
             <a href="{{ path('app_book_index') }}" class="text-decoration-none text-muted">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>
        </div>
    </div>
</div>
{% endblock %}


